query CreateHub($name: String!) { 
  users {
    me {
      createHub(name: $name) {
        id
        name
        url
        creator
        items {
          id
          url
          origin
        }
      }
    }
  }
}