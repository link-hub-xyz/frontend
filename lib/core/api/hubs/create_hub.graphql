query CreateHub($name: String!) { 
  users {
    me {
      createHub(name: $name) {
        id
        name
        url
        items {
          id
          url
          origin
        }
      }
    }
  }
}